import React from "react";

function ProjectDescriptionCard(props) {
  //   return <p className="">{props.value}</p>;
  //   return <p className="">{props.value[`${props.currentProjectId}`]}</p>;
  
  
  return <p className="">{props.value}</p>;
}

export default ProjectDescriptionCard;
